# 정렬 알고리즘 (Sorting Algorithms) 정리

## 1️⃣ 정렬이란?

**정렬(Sorting)**이란

> 데이터를 특정한 순서대로 나열하는 알고리즘
> 을 의미한다.

즉, **"데이터를 규칙에 따라 재배치하는"** 알고리즘이다.

---

## 2️⃣ 정렬 알고리즘의 분류

### 시간 복잡도에 따른 분류

| 알고리즘 | 최선 | 평균 | 최악 | 공간 | 안정성 |
|---------|------|------|------|------|--------|
| **병합 정렬** | O(N log N) | O(N log N) | O(N log N) | O(N) | ✅ |
| **퀵 정렬** | O(N log N) | O(N log N) | O(N²) | O(log N) | ❌ |
| **힙 정렬** | O(N log N) | O(N log N) | O(N log N) | O(1) | ❌ |
| **삽입 정렬** | O(N) | O(N²) | O(N²) | O(1) | ✅ |
| **선택 정렬** | O(N²) | O(N²) | O(N²) | O(1) | ❌ |
| **버블 정렬** | O(N²) | O(N²) | O(N²) | O(1) | ✅ |

### 안정성에 따른 분류

* **안정 정렬(Stable Sort)**: 같은 값의 상대적 순서 유지
  * 병합 정렬, 삽입 정렬, 버블 정렬
* **불안정 정렬(Unstable Sort)**: 같은 값의 상대적 순서가 바뀔 수 있음
  * 퀵 정렬, 힙 정렬, 선택 정렬

---

## 3️⃣ 주요 정렬 알고리즘

### 병합 정렬 (Merge Sort)

> 📖 **상세 설명**: [병합 정렬(Merge Sort) 정리](./merge-sort/merge-sort.md)

* **분할정복 알고리즘**: 배열을 반으로 나누어 각각 정렬한 후 병합
* **시간 복잡도**: O(N log N) (모든 경우)
* **공간 복잡도**: O(N)
* **안정 정렬**: ✅

### 퀵 정렬 (Quick Sort)

> 📖 **상세 설명**: [퀵 정렬(Quick Sort) 정리](./quick-sort/quick-sort.md)

* **분할정복 알고리즘**: 피벗을 기준으로 분할하여 정렬
* **시간 복잡도**: 평균 O(N log N), 최악 O(N²)
* **공간 복잡도**: O(log N)
* **안정 정렬**: ❌

---

## 4️⃣ 정렬 알고리즘 선택 기준

### 데이터 크기

* **작은 데이터 (N < 100)**: 삽입 정렬, 선택 정렬
* **중간 데이터 (100 ≤ N < 10,000)**: 퀵 정렬, 병합 정렬
* **큰 데이터 (N ≥ 10,000)**: 병합 정렬, 힙 정렬

### 안정성 요구사항

* **안정 정렬 필요**: 병합 정렬, 삽입 정렬
* **안정 정렬 불필요**: 퀵 정렬, 힙 정렬

### 최악의 경우 성능

* **최악의 경우도 보장 필요**: 병합 정렬, 힙 정렬
* **평균 성능 중시**: 퀵 정렬

## 5️⃣ Java에서의 정렬

### Arrays.sort()

```java
int[] arr = {5, 2, 8, 1, 9};
Arrays.sort(arr);  // 오름차순 정렬
```

* 기본 타입: 듀얼 피벗 퀵 정렬 (Dual-Pivot Quick Sort)
* 객체 타입: TimSort (병합 정렬 + 삽입 정렬)

### Collections.sort()

```java
List<Integer> list = new ArrayList<>();
list.add(5);
list.add(2);
Collections.sort(list);  // 오름차순 정렬
```

* TimSort 사용 (안정 정렬)

### 커스텀 정렬

```java
// Comparator를 사용한 커스텀 정렬
Arrays.sort(arr, (a, b) -> Integer.compare(b, a));  // 내림차순

// 객체 정렬
Arrays.sort(students, (a, b) -> {
    if (a.score != b.score) return b.score - a.score;
    return a.name.compareTo(b.name);
});
```

---

## 6️⃣ 정렬 관련 문제 해결 전략

### 1. 정렬이 필요한가?

* 데이터를 특정 순서로 나열해야 하는가?
* 정렬 후 탐색이 효율적인가?
* 정렬 후 인접 항목 비교가 필요한가?

### 2. 어떤 정렬 알고리즘을 사용할까?

* **학습 목적**: 병합 정렬, 퀵 정렬 직접 구현
* **실전 문제**: `Arrays.sort()`, `Collections.sort()` 사용
* **특수 요구사항**: 안정 정렬, 최악의 경우 성능 고려

### 3. 정렬 후 처리

* 정렬된 배열에서 이진 탐색
* 정렬된 배열에서 인접 항목 비교
* 정렬된 배열에서 투 포인터 활용

