# 알고리즘 선택 및 코드 설계 근거

대안 1: 브루트 포스 (모든 쌍 확인)

각 수에 대해 모든 두 수의 쌍을 확인하는 방법입니다. 시간 복잡도가 O(N³)이므로 N=2,000일 때 약 8×10⁹번의 연산이 필요하여 시간 제한(2초) 내에 해결할 수 없습니다.

---

대안 2: 정렬 + 투 포인터 (현재 방식) ✅

정렬 후 각 수에 대해 투 포인터로 두 수의 합을 찾는 방법입니다. 시간 복잡도는 O(N² log N)으로 시간 제한 내 해결 가능하고, 공간 복잡도는 O(N)으로 메모리 효율적입니다.

**메모리 사용량 계산 (N = 2,000):**

1. **Pair 객체 메모리:**
   - 객체 헤더: 12 bytes
   - long value: 8 bytes
   - int originalIdx: 4 bytes
   - **총: 24 bytes per Pair**

2. **실제 메모리 사용량:**
   - Pair 배열: 2,000 × 24 bytes = 48,000 bytes ≈ 47 KB
   - 입력 배열: 2,000 × 8 bytes = 16,000 bytes ≈ 16 KB
   - 기타 변수 및 스택: 약 10 KB
   - **총합: 약 73 KB**

**핵심 구현:**
- (값, 원래 인덱스) 쌍을 저장하여 정렬 후에도 원래 인덱스 추적 가능
- 투 포인터로 자기 자신을 제외하며 확인

```java
// (값, 원래 인덱스) 쌍 저장
Pair[] pairs = new Pair[N];
Arrays.sort(pairs, (a, b) -> Long.compare(a.value, b.value));

// 투 포인터로 확인
while (left < right) {
    if (left == i) { left++; continue; }  // 자기 자신 제외
    if (right == i) { right--; continue; }
    // 원래 인덱스로 자기 자신 확인 가능
}
```

**결론: ✅ 메모리 효율적(약 73 KB)이고 시간 제한 내 해결 가능**

---

대안 3: 해시맵 활용

모든 두 수의 합을 미리 계산하여 해시맵에 저장하고, 각 수에 대해 해시맵에서 O(1) 시간에 확인하는 방법입니다. 시간 복잡도 O(N²)로 빠르지만, 공간 복잡도 O(N²)로 메모리 사용량이 큽니다.

**메모리 사용량 계산 (N = 2,000):**

1. **최대 쌍 개수:** N × (N-1) / 2 = 2,000 × 1,999 / 2 = 1,999,000개

2. **각 엔트리 메모리:**
   - HashMap Node: 약 32 bytes (키, 값 참조, 해시값 등)
   - Long 키: 24 bytes (객체 헤더 12 + long 8 + 패딩 4)
   - ArrayList: 기본 16 bytes + 배열 참조 8 bytes
   - ArrayList 내부 배열: 쌍 개수에 따라 동적 할당
   - int[] 배열 (각 쌍): 24 bytes (객체 헤더 12 + int[2] 8 + 패딩 4)

3. **실제 메모리 사용량:**
   - HashMap 엔트리: 1,999,000 × 32 bytes ≈ 64 MB
   - Long 키: 1,999,000 × 24 bytes ≈ 48 MB
   - ArrayList 객체: 최대 1,999,000개 × 24 bytes ≈ 48 MB
   - ArrayList 내부 배열: 쌍 개수 × 8 bytes (참조) ≈ 16 MB
   - int[] 배열: 1,999,000 × 24 bytes ≈ 48 MB
   - **총합: 약 224 MB**

**문제점:**
- 메모리 제한 256 MB 대비 사용률: 224 MB / 256 MB ≈ 87.5%
- HashMap의 재해싱, 오버헤드 등을 고려하면 메모리 초과 가능성 높음
- 실제 테스트에서 메모리 초과 발생

**결론: ❌ 메모리 초과로 사용 불가**

---

대안 4: 정렬 + 이진 탐색

정렬 후 각 수에 대해 이진 탐색으로 필요한 수를 찾는 방법입니다. 시간 복잡도 O(N² log N)으로 시간 제한 내 해결 가능하지만, 해시맵보다 느립니다.

**결론:  사용 가능하지만 해시맵보다 느림**

---

### 📊 종합 비교

| 방식 | 시간 복잡도 | 공간 복잡도 | 구현 난이도 | 효율성 | 평가 |
|------|------------|------------|------------|--------|------|
| 브루트 포스 | O(N³) | O(N) | 쉬움 | ❌ 시간 초과 | ❌ |
| 정렬 + 투 포인터 | O(N² log N) | O(N) | 보통 | ✅ 사용 가능 | ✅ |
| 해시맵 | O(N²) | O(N²) | 쉬움 | ❌ 메모리 초과 | ❌ |
| 정렬 + 이진 탐색 | O(N² log N) | O(N) | 보통 | ✅ 사용 가능 | ⚠️ |

**결론:**
- 각 방식의 장단점을 고려한 결과
- **"정렬 + 투 포인터"가 가장 적합함**
- 시간 복잡도 O(N² log N)으로 시간 제한 내 해결 가능
- 공간 복잡도 O(N)으로 메모리 효율적
- (값, 원래 인덱스) 쌍을 저장하여 자기 자신 제외 처리 가능

