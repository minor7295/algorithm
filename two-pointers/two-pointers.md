
# 투 포인터 알고리즘(Two Pointers Algorithm) 정리

## 1️⃣ 투 포인터 알고리즘이란?

### 🌍 연속된 구간을 찾는 문제

**연속된 구간에서 특정 합을 찾는 문제:**

예를 들어, **15를 연속된 자연수의 합으로 나타내는 방법의 개수**를 구하는 문제를 생각해봅시다:

> **문제**: 
> - 자연수 15를 연속된 자연수들의 합으로 나타낼 수 있는 방법은 몇 가지일까요?
> - 예를 들어: 15 = 1+2+3+4+5, 15 = 4+5+6, 15 = 7+8, 15 = 15
> - 연속된 자연수 구간 중, 합이 정확히 15인 구간을 모두 찾아야 합니다

---

### 💡 왜 투 포인터 알고리즘이 필요한가?

**문제**: 연속된 구간을 찾는 문제를 어떻게 효율적으로 해결할까?

**완전 탐색 방법 (비효율적)**:
```
모든 구간을 다 확인:
- 구간 [1,1]: 합 = 1 (목표 아님)
- 구간 [1,2]: 합 = 1+2 = 3 (목표 아님)
- 구간 [1,3]: 합 = 1+2+3 = 6 (목표 아님)
- ...
- 구간 [1,15]: 합 = 1+2+...+15 = 120 (목표 아님)
- 구간 [2,2]: 합 = 2 (목표 아님)
- 구간 [2,3]: 합 = 2+3 = 5 (목표 아님)
- ...
총 15×15 = 225가지 구간을 모두 확인해야 함 ❌
→ 데이터가 1,000,000개면 1조 번의 연산 필요! (시간 초과)
```

**투 포인터 알고리즘 (효율적)**:
- **시작점**과 **끝점** 두 개의 위치만 사용
- 조건에 따라 위치를 이동하며 효율적으로 탐색
- 각 원소를 최대 2번만 방문 → **O(N) 시간 복잡도** ✅
- 데이터가 1,000,000개여도 약 200만 번의 연산으로 해결!

---

**투 포인터 알고리즘(Two Pointers Algorithm)**이란

> **시작점과 끝점을 나타내는 두 개의 위치를 사용하여 배열이나 수열을 효율적으로 탐색**하는 알고리즘으로,
> 두 위치를 적절히 이동시켜 문제를 해결하는 방법
> 을 의미한다.

**쉽게 이해하기**: "구간의 시작점과 끝점을 움직여가며 원하는 조건을 만족하는 구간을 찾는" 알고리즘입니다.

**핵심 아이디어**: 
- 구간의 **시작점**과 **끝점**을 나타내는 두 개의 변수만 사용
- 합이 작으면 끝점을 늘려서 구간을 넓힘
- 합이 크면 시작점을 늘려서 구간을 좁힘
- 합이 정확하면 방법을 찾음!

---

### 🎯 구체적인 예시: 연속된 자연수의 합

**문제**: 15를 연속된 자연수의 합으로 나타내는 방법의 개수를 구하세요.

**예를 들어**:
- 15 = 1+2+3+4+5 ✅
- 15 = 4+5+6 ✅
- 15 = 7+8 ✅
- 15 = 15 ✅

**투 포인터 방법 (효율적)**:
- **시작점(start)**: 구간의 시작 자연수 (예: 1, 2, 3, ...)
- **끝점(end)**: 구간의 끝 자연수 (예: 1, 2, 3, ...)
- **sum**: 현재 구간의 합

**핵심 아이디어**:
- 합이 15보다 작으면 → 끝점을 증가시켜 구간 확장 (합 증가)
- 합이 15보다 크면 → 시작점을 증가시켜 구간 축소 (합 감소)
- 합이 정확히 15이면 → 방법을 하나 찾음!

**단계별 과정 (목표: 15) - 핵심만 간단히**:

| 단계 | 시작 | 끝 | 구간 | 합 | 동작 |
|------|------|-----|------|-----|------|
| 초기 | 1 | 1 | - | 0 | 끝 증가 |
| 1 | 1 | 2 | [1] | 1 | 끝 증가 |
| 2 | 1 | 3 | [1,2] | 3 | 끝 증가 |
| 3 | 1 | 4 | [1,2,3] | 6 | 끝 증가 |
| 4 | 1 | 5 | [1,2,3,4] | 10 | 끝 증가 |
| 5 | 1 | 6 | [1,2,3,4,5] | **15** | ✅ 발견! |

**핵심 동작 원리**:
- **합이 작으면** (sum < 15) → 끝점을 증가시켜 구간 확장 (합 증가)
- **합이 크면** (sum > 15) → 시작점을 증가시켜 구간 축소 (합 감소)
- **합이 정확하면** (sum == 15) → 방법 발견!

**왜 작동하나?**
- 자연수는 모두 양수이기 때문에:
  - 끝점 증가 → 합 증가 ✅ (양수를 더하므로)
  - 시작점 증가 → 합 감소 ✅ (양수를 빼므로)
- 각 자연수는 최대 2번만 방문하므로 **O(N) 시간 복잡도**

---

## 2️⃣ 언제 사용할까?

투 포인터 알고리즘은 다음과 같은 상황에서 사용됩니다:

### ✅ 투 포인터를 사용할 수 있는 조건

1. **연속된 구간 탐색** (필수)
   - 연속된 부분 배열이나 수열을 다룰 때
   - 인덱스 순서가 유지되어야 함 (시간 순서, 배열 인덱스 순서)
   - 예: 연속된 시간대, 연속된 자연수, 연속된 배열 요소

2. **단조성(Monotonicity)** (필수)
   - 시작점과 끝점을 한 방향으로만 이동해도 해를 찾을 수 있을 때
   - **값이 모두 양수**이면 단조성이 보장됨:
     - 끝점 증가 → 합 증가 (양수를 더하므로)
     - 시작점 증가 → 합 감소 (양수를 빼므로)
   - **음수가 포함되면** 단조성이 깨져서 사용 불가

3. **정렬된 배열** (선택적, 다른 패턴)
   - Two Sum 같은 경우에는 정렬된 배열에서 양쪽에서 접근하는 패턴 사용
   - 연속된 구간 탐색과는 다른 패턴

### 📚 대표적인 예시

#### 1. 연속된 자연수의 합 (BOJ 2018)
**문제**: N을 연속된 자연수의 합으로 나타내는 방법의 개수
**전략**: 시작점과 끝점으로 연속된 구간의 합을 탐색
**시간 복잡도**: O(N)

#### 2. 두 수의 합 (Two Sum)
**문제**: 정렬된 배열에서 합이 target인 두 수 찾기
**전략**: 왼쪽 끝과 오른쪽 끝에서 시작하여 위치를 이동하며 탐색
**시간 복잡도**: O(N)

#### 3. 부분 수열의 합
**문제**: 연속된 부분 수열의 합이 특정 값인 경우 찾기
**전략**: 슬라이딩 윈도우와 유사하게 시작점과 끝점 이동
**시간 복잡도**: O(N)

---

## 3️⃣ 투 포인터 알고리즘의 핵심 개념

### 📌 기본 원리

**시작점과 끝점을 사용하여 구간을 탐색**

- **시작점(start)**: 구간의 시작 위치
- **끝점(end)**: 구간의 끝 위치
- **조건에 따라 위치 이동**: 
  - 합이 작으면 끝점을 늘려서 구간 확장
  - 합이 크면 시작점을 늘려서 구간 축소

### 📌 단조성(Monotonicity)

**시작점과 끝점을 한 방향으로만 이동해도 해를 찾을 수 있는 성질**

**핵심**: 
- **합이 작으면** → 끝점 증가 → 합 증가 (양수를 더하므로)
- **합이 크면** → 시작점 증가 → 합 감소 (양수를 빼므로)
- 따라서 **시작점과 끝점을 오른쪽으로만 이동해도 모든 경우를 탐색 가능** ✅

**조건**: 
- 시간 순서가 유지되고
- 모든 값이 양수이면
- 단조성이 보장되어 투 포인터 사용 가능

---

## 4️⃣ 투 포인터 알고리즘의 구조

### 기본 템플릿

```java
int twoPointers(int[] arr, int target) {
    int start = 0;
    int end = 0;
    int sum = 0;
    int count = 0;
    
    while (end < arr.length) {
        // 구간 확장: 끝점 이동
        if (sum < target) {
            sum += arr[end];
            end++;
        }
        // 구간 축소: 시작점 이동
        else if (sum > target) {
            sum -= arr[start];
            start++;
        }
        // 조건 만족: 해를 찾음
        else {
            count++;
            sum -= arr[start];
            start++;
        }
    }
    
    return count;
}
```

### 📝 코드 예시

```java
int N = 15;
int start = 1;      // 구간의 시작 자연수
int end = 1;        // 구간의 끝 자연수
int sum = 0;        // 현재 구간의 합
int count = 0;      // 방법의 개수

while (start <= N) {
    if (sum < N) {
        sum += end;  // 구간 확장
        end++;
    } else if (sum > N) {
        sum -= start;  // 구간 축소
        start++;
    } else {
        count++;       // 방법 발견!
        sum -= start;
        start++;
    }
}
```

**핵심**: 각 원소는 최대 2번 방문하므로 **O(N) 시간 복잡도** ✅

---

## 5️⃣ 투 포인터 알고리즘의 장단점

### ✅ 장점

#### 1. 효율적
- **시간 복잡도**: 보통 O(N) 또는 O(N log N)
- **예시**: 연속된 자연수의 합 문제는 O(N) - 각 원소를 최대 2번 방문
- 완전 탐색은 보통 O(N²) - 훨씬 느림

#### 2. 구현이 간단
- 시작점과 끝점 두 개의 변수만 관리하면 됨
- 복잡한 자료구조 불필요

#### 3. 메모리 효율
- 추가 메모리 사용이 적음 (보통 O(1) 또는 O(N))
- 완전 탐색은 보통 O(N²) 메모리 필요

### ❌ 단점

#### 1. 단조성 필요
**모든 문제에 적용할 수 없습니다!**

**중요한 구분**:
- ✅ **연속된 구간에서 값이 모두 양수이면 작동**: 
  - 예: 자연수 [1, 2, 3, 4, 5] (모두 양수)
  - 끝점 증가 → 합 증가 (양수를 더하므로)
  - 시작점 증가 → 합 감소 (양수를 빼므로)
  - 단조성이 보장되어 투 포인터 사용 가능 ✅

- ❌ **연속된 구간에 음수가 포함되면 작동 안 함**: 
  - 끝점을 증가시켜도 합이 감소할 수 있어서 단조성이 보장되지 않음

**예시: 음수가 포함된 경우**
```
배열: [3, -1, 4, 2, 5]
목표: 합이 6인 연속 구간 찾기

start=0, end=0: sum=3 < 6 → end 증가
start=0, end=1: sum=3+(-1)=2 < 6 → end 증가 (합이 감소함! ❌)
start=0, end=2: sum=2+4=6 == 6 ✅

하지만 이 경우:
- end를 증가시켰는데 합이 감소함 (3 → 2)
- 단조성이 보장되지 않아서 투 포인터로는 모든 경우를 찾을 수 없음
- 예를 들어, [3, -1, 4] = 6을 찾았지만, [-1, 4, 2, 1] = 6 같은 경우를 놓칠 수 있음
```

**단조성이 없는 경우**:
- 연속된 구간에 음수가 포함되면 끝점을 증가시켜도 합이 감소할 수 있음
- 이런 경우 해시맵이나 완전 탐색 필요

#### 2. 조건 만족 필요
- 시작점과 끝점 이동 규칙이 명확해야 함
- 조건이 복잡하면 적용 어려움

---

## 6️⃣ 투 포인터 vs 다른 알고리즘

### 📊 비교표

| 기법 | 특징 | 시간 복잡도 | 예시 | 적용 조건 |
|------|------|------------|------|----------|
| **투 포인터** | 시작점과 끝점으로 구간 탐색 | O(N) | 연속된 자연수의 합 | 단조성 필요 |
| **슬라이딩 윈도우** | 고정 크기 구간 탐색 | O(N) | 최대 부분 배열 합 | 고정 크기 |
| **완전 탐색** | 모든 경우 탐색 | O(N²) | 모든 부분 배열 | 단조성 불필요 |
| **이진 탐색** | 정렬된 배열 탐색 | O(log N) | 특정 값 찾기 | 정렬 필요 |

### 🔍 구체적인 비교: 연속된 자연수의 합

#### 상황: 15를 연속된 자연수의 합으로 나타내는 방법의 개수

**1. 투 포인터 방법**:
- **동작**: 시작점과 끝점을 이동하며 탐색
- **방문 횟수**: 각 자연수(1~15)를 최대 2번 방문
  - 예: 1은 시작점으로 한 번, 끝점으로 한 번 방문
- **시간 복잡도**: O(15) = 약 30번의 연산
- **코드**: 간단한 while문과 if문만 사용
- **장점**: 구현이 간단하고 이해하기 쉬움

**2. 완전 탐색 방법**:
- **동작**: 모든 (시작점, 끝점) 쌍을 확인
- **확인 횟수**: 
  - 시작점 1: (1,1), (1,2), ..., (1,15) → 15번
  - 시작점 2: (2,2), (2,3), ..., (2,15) → 14번
  - ... 시작점 15: (15,15) → 1번
  - 총: 15+14+...+1 = 120번
- **시간 복잡도**: O(15²) = 약 225번의 연산
- **코드**: 이중 for문 필요
- **단점**: N이 커지면 매우 느림 (N=1,000,000이면 1조 번 연산)

**3. 수학적 접근**:
- **동작**: 등차수열 공식을 이용한 수학적 계산
- **시간 복잡도**: O(√15) = 약 4번의 연산
- **코드**: 복잡한 수학 공식과 약수 계산 필요
- **단점**: 이해하기 어렵고 구현이 복잡

| 방법 | 연산 횟수 (N=15) | 연산 횟수 (N=1,000,000) | 코드 난이도 | 이해도 |
|------|----------------|----------------------|-----------|--------|
| **투 포인터** | 약 30번 | 약 2,000,000번 | ⭐ 간단 | ⭐⭐⭐ 쉬움 |
| **완전 탐색** | 약 225번 | 약 1조 번 ❌ | ⭐⭐ 보통 | ⭐⭐ 보통 |
| **수학적 접근** | 약 4번 | 약 2,000번 | ⭐⭐⭐ 복잡 | ⭐ 어려움 |

**결론**: **투 포인터가 구현이 간단하면서도 충분히 효율적**입니다!

---

## 7️⃣ 투 포인터 알고리즘 구현 패턴

### 패턴 1: 구간 확장/축소 (연속된 자연수의 합)

**사용 시나리오**: 연속된 구간의 합이 특정 값인 경우 찾기

```java
int start = 1;
int end = 1;
int sum = 0;
int count = 0;

while (start <= N) {
    if (sum < N) {
        // 구간 확장
        sum += end;
        end++;
    } else if (sum > N) {
        // 구간 축소
        sum -= start;
        start++;
    } else {
        // 조건 만족
        count++;
        sum -= start;
        start++;
    }
}
```

### 패턴 2: 양쪽에서 접근 (Two Sum)

**사용 시나리오**: 정렬된 배열에서 합이 target인 두 수 찾기

**구체적인 예시**: 
- 배열: [2, 7, 11, 15] (오름차순 정렬됨)
- 목표: 합이 9인 두 수 찾기

**과정**:
```
초기: 왼쪽 끝(left=0, 값=2), 오른쪽 끝(right=3, 값=15)
합 = 2 + 15 = 17 > 9 → 합이 크므로 오른쪽 끝을 왼쪽으로 이동
→ right=2 (값=11)

합 = 2 + 11 = 13 > 9 → 합이 크므로 오른쪽 끝을 왼쪽으로 이동
→ right=1 (값=7)

합 = 2 + 7 = 9 == 9 → 해를 찾음! ✅
→ [0, 1] 반환 (값: 2, 7)
```

**왜 이렇게 하면 될까?**
- 배열이 정렬되어 있으므로:
  - 왼쪽 끝을 오른쪽으로 이동 → 합 증가
  - 오른쪽 끝을 왼쪽으로 이동 → 합 감소
- 따라서 합이 작으면 왼쪽을 이동, 합이 크면 오른쪽을 이동하면 됨

```java
int left = 0;                    // 왼쪽 끝 위치
int right = arr.length - 1;      // 오른쪽 끝 위치

while (left < right) {
    int sum = arr[left] + arr[right];
    
    if (sum < target) {
        left++;  // 합이 작으면 왼쪽 끝을 오른쪽으로 이동 (합 증가)
    } else if (sum > target) {
        right--;  // 합이 크면 오른쪽 끝을 왼쪽으로 이동 (합 감소)
    } else {
        // 해를 찾음
        return new int[]{left, right};
    }
}
```

### 패턴 3: 슬라이딩 윈도우

**사용 시나리오**: 고정 크기 구간 탐색

```java
int windowSum = 0;
int maxSum = Integer.MIN_VALUE;

// 초기 윈도우
for (int i = 0; i < k; i++) {
    windowSum += arr[i];
}

// 윈도우 이동
for (int i = k; i < arr.length; i++) {
    windowSum = windowSum - arr[i - k] + arr[i];
    maxSum = Math.max(maxSum, windowSum);
}
```

---

## 8️⃣ 정리

투 포인터 알고리즘은 **시작점과 끝점을 나타내는 두 개의 위치를 사용하여 배열이나 수열을 효율적으로 탐색**하는 방법입니다.

### ✅ 핵심 요약

1. **기본 원리**: 시작점과 끝점 두 개의 위치로 구간을 탐색
2. **적용 조건**: 
   - 연속된 구간 탐색
   - 단조성(Monotonicity) 필요
3. **장점**: 구현 간단, 효율적(O(N)), 메모리 효율
4. **단점**: 단조성이 없으면 사용 불가

### 🎯 실전 팁

1. **문제 분석**: 연속된 구간을 다루는지 확인
2. **단조성 확인**: 시작점과 끝점을 한 방향으로만 이동해도 해를 찾을 수 있는지 확인
3. **구현**: 시작점과 끝점을 조건에 따라 이동
4. **테스트**: 다양한 예시로 검증

### 📚 학습 순서

1. **연속된 자연수의 합 (BOJ 2018)** - 가장 기본적인 투 포인터 문제
2. **Two Sum** - 양쪽에서 접근하는 패턴
3. **슬라이딩 윈도우** - 고정 크기 구간 탐색

적절한 문제에 투 포인터 알고리즘을 적용하면 **효율적이고 간단한 해결책**을 얻을 수 있습니다!

