# 수들의 합 5

## 📌 문제 요약

어떠한 자연수 N은, 몇 개의 연속된 자연수의 합으로 나타낼 수 있다. 어떤 자연수 N(1 ≤ N ≤ 10,000,000)에 대해서, 이 N을 몇 개의 연속된 자연수의 합으로 나타내는 가지수를 구하는 프로그램을 작성하시오.

이때, 사용하는 자연수는 N이하여야 한다.

---

## 🔍 문제 설명

* 자연수 N을 연속된 자연수의 합으로 나타내는 방법의 개수를 구해야 한다.
* 연속된 자연수는 N 이하여야 한다.
* 예를 들어, 15를 나타내는 방법은:
  - 15 (자기 자신)
  - 7 + 8
  - 4 + 5 + 6
  - 1 + 2 + 3 + 4 + 5
  - 총 4가지

---

## 📥 입력 조건

* 첫 줄에 정수 N이 주어진다. (1 ≤ N ≤ 10,000,000)

---

## 📤 출력 조건

* 입력된 자연수 N을 몇 개의 연속된 자연수의 합으로 나타내는 가지수를 출력한다.

---

## 💡 문제 핵심 해석

이 문제는 **투 포인터 알고리즘(Two Pointers Algorithm)**을 사용하여 해결할 수 있는 전형적인 연속 구간 탐색 문제이다.

### 핵심 조건

* **연속된 자연수**: 시작점부터 끝점까지 연속된 자연수들의 합
* **자연수는 N 이하**: 사용하는 자연수는 모두 N 이하여야 함
* **모든 방법 찾기**: N을 나타내는 모든 방법의 개수를 구해야 함

### 투 포인터 접근 가능성

연속된 자연수의 합을 구하는 문제이므로 **투 포인터 알고리즘이 적합**하다:

* **start 포인터**: 연속된 구간의 시작 자연수
* **end 포인터**: 연속된 구간의 끝 자연수
* **sum**: 현재 구간의 합
* **조건에 따라 포인터 이동**:
  - `sum < N`이면 `end` 증가 (구간 확장)
  - `sum > N`이면 `start` 증가 (구간 축소)
  - `sum == N`이면 방법 개수 증가

예를 들어, N = 15인 경우:

```
start=1, end=1: sum=1 < 15 → end 증가
start=1, end=2: sum=3 < 15 → end 증가
start=1, end=3: sum=6 < 15 → end 증가
start=1, end=4: sum=10 < 15 → end 증가
start=1, end=5: sum=15 == 15 → 방법 1개 발견! (1+2+3+4+5)
start=2, end=5: sum=14 < 15 → end 증가
start=2, end=6: sum=20 > 15 → start 증가
start=3, end=6: sum=18 > 15 → start 증가
start=4, end=6: sum=15 == 15 → 방법 1개 발견! (4+5+6)
...
```

➡️ **투 포인터 알고리즘으로 모든 연속 구간을 효율적으로 탐색**할 수 있다.

---

## 🧠 해결 전략 개요

1. **투 포인터 초기화**: `start = 1`, `end = 1`, `sum = 0`
2. **구간 탐색 반복**:
   - `sum < N`이면 `end` 증가하여 구간 확장
   - `sum > N`이면 `start` 증가하여 구간 축소
   - `sum == N`이면 방법 개수 증가 후 `start` 증가
3. **종료 조건**: `start > N`이 될 때까지 반복

---

## ✨ 예시

### 예시 1: 문제에서 제공한 예제

**입력**
```
15
```

**처리 과정**
```
start=1, end=1: sum=0 → sum=1, end=2 (sum < 15)
start=1, end=2: sum=1 → sum=3, end=3 (sum < 15)
start=1, end=3: sum=3 → sum=6, end=4 (sum < 15)
start=1, end=4: sum=6 → sum=10, end=5 (sum < 15)
start=1, end=5: sum=10 → sum=15, end=6 (sum == 15) → count=1
start=2, end=6: sum=14 → sum=20, end=7 (sum > 15)
start=3, end=7: sum=18 → sum=15, start=4 (sum > 15)
start=4, end=7: sum=15 (sum == 15) → count=2
start=5, end=7: sum=11 → sum=18, end=8 (sum > 15)
start=6, end=8: sum=21 → sum=15, start=7 (sum > 15)
start=7, end=8: sum=15 (sum == 15) → count=3
start=8, end=8: sum=8 → sum=23, end=9 (sum > 15)
start=9, end=9: sum=9 → sum=24, end=10 (sum > 15)
...
start=15, end=15: sum=15 (sum == 15) → count=4
```

**발견된 방법**:
1. 15 (자기 자신)
2. 1 + 2 + 3 + 4 + 5
3. 4 + 5 + 6
4. 7 + 8

**출력**
```
4
```

### 예시 2: N = 10

**입력**
```
10
```

**처리 과정**
```
start=1, end=1: sum=1, end=2 (sum < 10)
start=1, end=2: sum=3, end=3 (sum < 10)
start=1, end=3: sum=6, end=4 (sum < 10)
start=1, end=4: sum=10 (sum == 10) → count=1 (1+2+3+4)
start=2, end=4: sum=9, end=5 (sum < 10)
start=2, end=5: sum=14, start=3 (sum > 10)
start=3, end=5: sum=12, start=4 (sum > 10)
start=4, end=5: sum=9, end=6 (sum < 10)
start=4, end=6: sum=15, start=5 (sum > 10)
...
start=10, end=10: sum=10 (sum == 10) → count=2
```

**발견된 방법**:
1. 10 (자기 자신)
2. 1 + 2 + 3 + 4

**출력**
```
2
```

---

## 📝 정리

이 문제는 **투 포인터 알고리즘**을 사용하여 해결할 수 있는 전형적인 연속 구간 탐색 문제이다.

* **핵심 조건**: 연속된 자연수의 합으로 N을 나타내는 방법의 개수
* **전략**: start와 end 포인터로 연속 구간을 탐색하며 합이 N인 경우 찾기
* **시간 복잡도**: O(N) - 각 자연수를 최대 2번 방문
* **공간 복잡도**: O(1) - 추가 메모리 사용 없음

