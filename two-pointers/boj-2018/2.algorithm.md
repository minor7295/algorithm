# 알고리즘: 투 포인터 알고리즘 (Two Pointers Algorithm)

> 📖 **전체 알고리즘 설명**: [투 포인터 알고리즘 정리](../two-pointers.md)

이 문제는 **투 포인터 알고리즘(Two Pointers Algorithm)**을 사용하여 해결합니다.  
상위 폴더의 [투 포인터 알고리즘 정리 문서](../two-pointers.md)에서 알고리즘의 전체 이론과 개념을 확인할 수 있습니다.

---

## 이 문제에서의 투 포인터 적용

### 문제 특성

* **연속된 자연수의 합**: 시작점부터 끝점까지 연속된 자연수들의 합
* **모든 방법 찾기**: N을 나타내는 모든 방법의 개수를 구해야 함
* **자연수는 N 이하**: 사용하는 자연수는 모두 N 이하여야 함
* **입력 크기**: N ≤ 10,000,000 (큰 입력 크기)

---

## 이 문제의 특이사항

### 1️⃣ 투 포인터 알고리즘 적용 가능 조건

이 문제에서 투 포인터 알고리즘이 적합한 이유:

* **연속된 구간 탐색**: 연속된 자연수의 합을 구하는 문제
* **단조성(Monotonicity)**: 
  - `sum < N`이면 `end` 증가 → sum이 증가 (단조 증가)
  - `sum > N`이면 `start` 증가 → sum이 감소 (단조 감소)
* **효율성**: 완전 탐색 O(N²)보다 훨씬 빠른 O(N) 시간 복잡도

### 2️⃣ 왜 투 포인터가 효율적인가?

**완전 탐색 방법**:
- 모든 시작점과 끝점을 확인: O(N²)
- N = 10,000,000이면 약 10¹⁴번의 연산 필요 ❌

**투 포인터 방법**:
- 각 자연수를 최대 2번 방문: O(N)
- N = 10,000,000이면 약 10⁷번의 연산 필요 ✅

**예시:**
```
N = 15

완전 탐색: 모든 (start, end) 쌍 확인
- (1,1), (1,2), ..., (1,15)
- (2,2), (2,3), ..., (2,15)
- ...
- 총 O(N²) = O(225) 연산

투 포인터: start와 end를 조건에 따라 이동
- start=1, end=1 → end 증가
- start=1, end=5 → sum==15 발견
- start=2, end=5 → end 증가
- start=2, end=6 → start 증가
- ...
- 총 O(N) = O(15) 연산
```

### 3️⃣ 시간 복잡도

* **각 자연수 방문 횟수**: 최대 2번 (start와 end가 각각 한 번씩)
* **전체 시간 복잡도**: **O(N)**

제약 조건 `N ≤ 10,000,000`으로 인해 O(N) 시간 복잡도가 필요합니다.

### 4️⃣ 공간 복잡도

* **추가 메모리**: 포인터 변수와 합 변수만 사용
* **전체 공간 복잡도**: **O(1)**

---

## 핵심 구현 패턴

이 문제에 특화된 투 포인터 구현:

### 방법 1: 기본 투 포인터 (권장) ✅

```java
int start = 1;
int end = 1;
int sum = 0;
int count = 0;

while (start <= N) {
    if (sum < N) {
        // 구간 확장: end 포인터 이동
        sum += end;
        end++;
    } else if (sum > N) {
        // 구간 축소: start 포인터 이동
        sum -= start;
        start++;
    } else {
        // 조건 만족: 해를 찾음
        count++;
        sum -= start;
        start++;
    }
}

System.out.println(count);
```

### 방법 2: while 루프 최적화

```java
int start = 1;
int end = 1;
int sum = 0;
int count = 0;

while (start <= N) {
    // 구간 확장
    while (sum < N && end <= N) {
        sum += end;
        end++;
    }
    
    // 조건 확인
    if (sum == N) {
        count++;
    }
    
    // 구간 축소
    sum -= start;
    start++;
}

System.out.println(count);
```

### 방법 3: 수학적 접근 (참고)

```java
int count = 0;

// k개의 연속된 자연수: a, a+1, ..., a+k-1
// 합 = k*(2a+k-1)/2 = N
// 2N = k*(2a+k-1)
// k는 2N의 약수이면서 2a+k-1 > k (a >= 1)를 만족해야 함

for (int k = 1; k * k <= 2 * N; k++) {
    if (2 * N % k == 0) {
        int m = 2 * N / k;
        if ((m - k + 1) % 2 == 0 && (m - k + 1) / 2 >= 1) {
            count++;
        }
    }
}

System.out.println(count);
```

---

## 다른 연속 구간 문제와의 차이점

| 특징 | 일반 부분 수열 합 | 이 문제 (2018) |
|------|-----------------|---------------|
| 자연수 범위 | 제한 없음 | N 이하 |
| 시작점 | 0 또는 1 | 1부터 |
| 종료 조건 | end < 배열 길이 | start <= N |
| 시간 복잡도 | O(N) | O(N) |

---

## 주의사항

### 1️⃣ 오버플로우 방지

* N은 최대 10,000,000이므로 `int` 타입으로 충분
* sum도 최대 약 N²/2이지만, 실제로는 N보다 크지 않으므로 `int` 타입으로 충분

### 2️⃣ 종료 조건

* `start <= N`일 때까지 반복
* `end > N`이 되면 더 이상 구간을 확장할 수 없으므로 자동으로 start만 증가

### 3️⃣ 자기 자신 포함

* N = N (자기 자신)도 하나의 방법이므로 포함됨
* 투 포인터 알고리즘이 자동으로 처리

---

## 참고

* 전체 투 포인터 알고리즘 설명: [../two-pointers.md](../two-pointers.md)
* 문제 분석: [1.analysis.md](./1.analysis.md)
* 알고리즘 적용 근거: [3.reasoning.md](./3.reasoning.md)

