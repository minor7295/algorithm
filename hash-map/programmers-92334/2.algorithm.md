# 알고리즘: 해시맵(HashMap)과 집합(Set)

> 📖 **전체 알고리즘 설명**: [해시맵 정리](../hash-map.md)

이 문제는 **해시맵(HashMap)과 집합(Set)**을 사용하여 해결합니다.  
상위 폴더의 [해시맵 정리 문서](../hash-map.md)에서 알고리즘의 전체 이론과 개념을 확인할 수 있습니다.

---

## 이 문제에서의 해시맵 적용

### 문제 특성

* **중복 제거**: 같은 유저가 같은 유저를 여러 번 신고해도 1회로 처리
* **카운팅**: 각 유저가 신고당한 횟수를 세어야 함
* **그룹핑**: 각 유저가 신고한 유저 목록을 저장해야 함
* **빠른 조회**: 유저 ID로 빠르게 값을 조회해야 함

---

## 이 문제의 특이사항

### 1️⃣ 중복 신고 제거 (Set 사용)

**문제**: 같은 유저가 같은 유저를 여러 번 신고해도 1회로 처리해야 함

**해결**: `Set<String>`을 사용하여 중복 신고 제거

```java
Set<String> uniqueReports = new HashSet<>();
for (String report : report) {
    uniqueReports.add(report);  // 중복 자동 제거
}
```

**예시**:
```
입력: ["ryan con", "ryan con", "ryan con", "ryan con"]
출력: {"ryan con"}  // 중복 제거됨
```

**시간 복잡도**: O(R) - report 배열 길이에 비례
**공간 복잡도**: O(R) - 최대 R개의 고유한 신고 저장

---

### 2️⃣ 신고 횟수 카운트 (Map 사용)

**문제**: 각 유저가 신고당한 횟수를 세어야 함

**해결**: `Map<String, Integer>`를 사용하여 카운팅

```java
Map<String, Integer> reportCount = new HashMap<>();
for (String report : uniqueReports) {
    String[] parts = report.split(" ");
    String reported = parts[1];  // 신고당한 유저
    
    reportCount.put(reported, 
        reportCount.getOrDefault(reported, 0) + 1);
}
```

**예시**:
```
입력: {"muzi frodo", "apeach frodo", "frodo neo", "muzi neo"}
출력: {
  "frodo": 2,
  "neo": 2,
  "muzi": 0,
  "apeach": 0
}
```

**시간 복잡도**: O(R) - 고유한 신고 수에 비례
**공간 복잡도**: O(N) - 유저 수에 비례

---

### 3️⃣ 정지된 유저 확인 (Set 사용)

**문제**: k번 이상 신고된 유저를 찾아야 함

**해결**: 신고 횟수 맵을 순회하며 k 이상인 유저를 `Set<String>`에 저장

```java
Set<String> bannedUsers = new HashSet<>();
for (Map.Entry<String, Integer> entry : reportCount.entrySet()) {
    if (entry.getValue() >= k) {
        bannedUsers.add(entry.getKey());
    }
}
```

**예시**:
```
입력: reportCount = {"frodo": 2, "neo": 2, "muzi": 1}, k = 2
출력: {"frodo", "neo"}  // 2회 이상 신고당한 유저
```

**시간 복잡도**: O(N) - 유저 수에 비례
**공간 복잡도**: O(N) - 최대 N개의 정지된 유저 저장

---

### 4️⃣ 유저별 신고한 유저 목록 저장 (Map + Set 사용)

**문제**: 각 유저가 신고한 유저 목록을 저장해야 함

**해결**: `Map<String, Set<String>>`를 사용하여 그룹핑

```java
Map<String, Set<String>> userReports = new HashMap<>();
for (String report : uniqueReports) {
    String[] parts = report.split(" ");
    String reporter = parts[0];   // 신고한 유저
    String reported = parts[1];   // 신고당한 유저
    
    userReports.computeIfAbsent(reporter, k -> new HashSet<>())
                .add(reported);
}
```

**예시**:
```
입력: {"muzi frodo", "apeach frodo", "frodo neo", "muzi neo"}
출력: {
  "muzi": {"frodo", "neo"},
  "apeach": {"frodo"},
  "frodo": {"neo"}
}
```

**시간 복잡도**: O(R) - 고유한 신고 수에 비례
**공간 복잡도**: O(N + R) - 유저 수와 신고 수에 비례

---

### 5️⃣ 메일 수 계산 (Set 교집합)

**문제**: 각 유저가 신고한 유저 중 정지된 유저의 수를 세어야 함

**해결**: 각 유저가 신고한 유저 집합과 정지된 유저 집합의 교집합 개수 계산

```java
int[] answer = new int[id_list.length];
for (int i = 0; i < id_list.length; i++) {
    String user = id_list[i];
    Set<String> reported = userReports.getOrDefault(user, new HashSet<>());
    
    int mailCount = 0;
    for (String reportedUser : reported) {
        if (bannedUsers.contains(reportedUser)) {
            mailCount++;
        }
    }
    answer[i] = mailCount;
}
```

**예시**:
```
입력: 
  userReports = {"muzi": {"frodo", "neo"}, "frodo": {"neo"}}
  bannedUsers = {"frodo", "neo"}

출력:
  "muzi": {"frodo", "neo"} ∩ {"frodo", "neo"} = 2개
  "frodo": {"neo"} ∩ {"frodo", "neo"} = 1개
```

**시간 복잡도**: O(N × M) - 유저 수 × 평균 신고한 유저 수
**공간 복잡도**: O(1) - 추가 공간 사용 없음

---

## 시간 복잡도

### 전체 시간 복잡도 분석

| 단계 | 연산 | 시간 복잡도 | 설명 |
|------|------|------------|------|
| 1. 중복 제거 | Set에 추가 | O(R) | report 배열 길이에 비례 |
| 2. 신고 횟수 카운트 | Map 조회/수정 | O(R) | 고유한 신고 수에 비례 |
| 3. 정지된 유저 확인 | Map 순회 | O(N) | 유저 수에 비례 |
| 4. 유저별 신고 목록 저장 | Map 조회/수정 | O(R) | 고유한 신고 수에 비례 |
| 5. 메일 수 계산 | Set 교집합 | O(N × M) | 유저 수 × 평균 신고한 유저 수 |

**전체 시간 복잡도**: **O(R + N × M)**
- R: report 배열 길이 (최대 200,000)
- N: id_list 길이 (최대 1,000)
- M: 평균 신고한 유저 수 (보통 작음)

**실제로는**: O(R)이 지배적 (R이 N보다 훨씬 큼)

---

## 공간 복잡도

### 전체 공간 복잡도 분석

| 자료구조 | 공간 복잡도 | 설명 |
|---------|-----------|------|
| `uniqueReports` (Set) | O(R) | 최대 R개의 고유한 신고 저장 |
| `reportCount` (Map) | O(N) | 유저 수에 비례 |
| `bannedUsers` (Set) | O(N) | 최대 N개의 정지된 유저 저장 |
| `userReports` (Map) | O(N + R) | 유저 수 + 신고 수에 비례 |
| `answer` (배열) | O(N) | 유저 수에 비례 |

**전체 공간 복잡도**: **O(N + R)**
- N: id_list 길이 (최대 1,000)
- R: report 배열 길이 (최대 200,000)

---

## 핵심 구현 패턴

이 문제에 특화된 해시맵 구현 방법을 비교합니다.

### 📊 방법 비교표

| 비교 기준 | 방법 1: 단계별 처리 | 방법 2: 통합 처리 | 방법 3: 스트림 API |
|----------|------------------|-----------------|------------------|
| **시간 복잡도** | O(R + N × M) | O(R + N × M) | O(R + N × M) |
| **공간 복잡도** | O(N + R) | O(N + R) | O(N + R) |
| **가독성** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **디버깅** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ |
| **유지보수** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **권장도** | ✅ **권장** | ⚠️ 비권장 | ✅ 권장 |

---

### 방법 1: 단계별 처리 (권장) ✅

```java
// 1. 중복 신고 제거
Set<String> uniqueReports = new HashSet<>();
for (String report : report) {
    uniqueReports.add(report);
}

// 2. 신고 횟수 카운트
Map<String, Integer> reportCount = new HashMap<>();
for (String report : uniqueReports) {
    String[] parts = report.split(" ");
    String reported = parts[1];
    reportCount.put(reported, 
        reportCount.getOrDefault(reported, 0) + 1);
}

// 3. 정지된 유저 확인
Set<String> bannedUsers = new HashSet<>();
for (Map.Entry<String, Integer> entry : reportCount.entrySet()) {
    if (entry.getValue() >= k) {
        bannedUsers.add(entry.getKey());
    }
}

// 4. 유저별 신고한 유저 목록 저장
Map<String, Set<String>> userReports = new HashMap<>();
for (String report : uniqueReports) {
    String[] parts = report.split(" ");
    String reporter = parts[0];
    String reported = parts[1];
    userReports.computeIfAbsent(reporter, k -> new HashSet<>())
                .add(reported);
}

// 5. 메일 수 계산
int[] answer = new int[id_list.length];
for (int i = 0; i < id_list.length; i++) {
    String user = id_list[i];
    Set<String> reported = userReports.getOrDefault(user, new HashSet<>());
    int mailCount = 0;
    for (String reportedUser : reported) {
        if (bannedUsers.contains(reportedUser)) {
            mailCount++;
        }
    }
    answer[i] = mailCount;
}
```

#### ✅ 장점

1. **가독성**: 각 단계가 명확하게 분리되어 이해하기 쉬움
2. **디버깅**: 각 단계별로 중간 결과를 확인하기 쉬움
3. **유지보수**: 단계별로 수정하기 쉬움
4. **명확성**: 알고리즘의 의도가 코드에 잘 드러남

#### ❌ 단점

1. **코드 길이**: 다소 길어질 수 있음
2. **반복**: report 배열을 여러 번 순회

#### 💡 언제 사용?

- **권장**: 초보자에게 적합, 가독성과 유지보수성 중시
- 이 문제의 경우 **가장 적합한 방법**

---

### 방법 2: 통합 처리

```java
Map<String, Integer> reportCount = new HashMap<>();
Map<String, Set<String>> userReports = new HashMap<>();
Set<String> uniqueReports = new HashSet<>();

for (String report : report) {
    if (uniqueReports.add(report)) {  // 중복 제거
        String[] parts = report.split(" ");
        String reporter = parts[0];
        String reported = parts[1];
        
        // 신고 횟수 카운트
        reportCount.put(reported, 
            reportCount.getOrDefault(reported, 0) + 1);
        
        // 유저별 신고 목록 저장
        userReports.computeIfAbsent(reporter, k -> new HashSet<>())
                    .add(reported);
    }
}

// 정지된 유저 확인 및 메일 수 계산
Set<String> bannedUsers = new HashSet<>();
for (Map.Entry<String, Integer> entry : reportCount.entrySet()) {
    if (entry.getValue() >= k) {
        bannedUsers.add(entry.getKey());
    }
}

int[] answer = new int[id_list.length];
for (int i = 0; i < id_list.length; i++) {
    String user = id_list[i];
    Set<String> reported = userReports.getOrDefault(user, new HashSet<>());
    int mailCount = 0;
    for (String reportedUser : reported) {
        if (bannedUsers.contains(reportedUser)) {
            mailCount++;
        }
    }
    answer[i] = mailCount;
}
```

#### ✅ 장점

1. **효율성**: report 배열을 한 번만 순회
2. **간결성**: 코드가 더 짧음

#### ❌ 단점

1. **가독성**: 여러 작업이 한 루프에 섞여 있어 이해하기 어려움
2. **디버깅**: 중간 결과를 확인하기 어려움
3. **유지보수**: 수정 시 여러 부분을 고려해야 함

#### 💡 언제 사용?

- **비권장**: 이 문제에서는 가독성이 더 중요
- 성능이 매우 중요한 경우에만 고려

---

### 방법 3: 스트림 API (권장) ✅

```java
// 중복 신고 제거
Set<String> uniqueReports = Arrays.stream(report)
    .collect(Collectors.toSet());

// 신고 횟수 카운트
Map<String, Long> reportCount = uniqueReports.stream()
    .map(r -> r.split(" ")[1])
    .collect(Collectors.groupingBy(Function.identity(), Collectors.counting()));

// 정지된 유저 확인
Set<String> bannedUsers = reportCount.entrySet().stream()
    .filter(e -> e.getValue() >= k)
    .map(Map.Entry::getKey)
    .collect(Collectors.toSet());

// 유저별 신고 목록 저장
Map<String, Set<String>> userReports = uniqueReports.stream()
    .collect(Collectors.groupingBy(
        r -> r.split(" ")[0],
        Collectors.mapping(r -> r.split(" ")[1], Collectors.toSet())
    ));

// 메일 수 계산
int[] answer = Arrays.stream(id_list)
    .mapToInt(user -> {
        Set<String> reported = userReports.getOrDefault(user, Collections.emptySet());
        return (int) reported.stream()
            .filter(bannedUsers::contains)
            .count();
    })
    .toArray();
```

#### ✅ 장점

1. **선언적**: 무엇을 하는지 명확하게 표현
2. **간결성**: 코드가 더 짧고 읽기 쉬움
3. **함수형**: 함수형 프로그래밍 스타일

#### ❌ 단점

1. **성능**: 약간의 오버헤드가 있을 수 있음
2. **디버깅**: 중간 결과를 확인하기 어려움
3. **학습 곡선**: 스트림 API에 익숙해야 함

#### 💡 언제 사용?

- **권장**: 스트림 API에 익숙한 경우
- 함수형 프로그래밍 스타일을 선호하는 경우

---

### 🎯 최종 권장사항

이 문제(프로그래머스 92334)의 경우:

1. **방법 1 (단계별 처리)**: **가장 권장** ⭐
   - 가독성과 유지보수성이 가장 좋음
   - 초보자도 이해하기 쉬움
   - 디버깅이 쉬움

2. **방법 3 (스트림 API)**: **권장** ⭐
   - 스트림 API에 익숙한 경우
   - 코드가 더 간결함

3. **방법 2 (통합 처리)**: **비권장** ⚠️
   - 가독성이 떨어짐
   - 디버깅이 어려움

---

## 다른 해시맵 문제와의 차이점

| 특징 | 일반 카운팅 문제 | 이 문제 (92334) |
|------|----------------|----------------|
| 중복 처리 | 없음 | Set으로 중복 제거 필요 |
| 그룹핑 | 없음 | 유저별 신고 목록 저장 필요 |
| 교집합 연산 | 없음 | 정지된 유저와 교집합 계산 필요 |
| 복잡도 | 단순 카운팅 | 다단계 처리 |

### 단순 카운팅 문제와의 차이

**단순 카운팅 문제**:
```java
Map<String, Integer> count = new HashMap<>();
for (String item : items) {
    count.put(item, count.getOrDefault(item, 0) + 1);
}
```

**이 문제**:
- 중복 제거 → 카운팅 → 그룹핑 → 교집합 계산
- 여러 단계를 거쳐야 함

➡️ 이 문제는 **해시맵의 여러 패턴을 조합**하여 해결하는 문제입니다.

---

## 주의사항

### 1️⃣ 중복 신고 처리

**문제**: 같은 유저가 같은 유저를 여러 번 신고해도 1회로 처리해야 함

**해결**: `Set<String>`을 사용하여 중복 제거

```java
Set<String> uniqueReports = new HashSet<>();
for (String report : report) {
    uniqueReports.add(report);  // 중복 자동 제거
}
```

### 2️⃣ null 처리

**문제**: `get()` 메서드는 키가 없으면 `null`을 반환

**해결**: `getOrDefault()` 사용

```java
Set<String> reported = userReports.getOrDefault(user, new HashSet<>());
```

### 3️⃣ id_list 순서 유지

**문제**: 결과 배열은 `id_list` 순서대로 반환해야 함

**해결**: `id_list`를 순회하며 메일 수 계산

```java
int[] answer = new int[id_list.length];
for (int i = 0; i < id_list.length; i++) {
    String user = id_list[i];
    // 메일 수 계산
    answer[i] = mailCount;
}
```

---

## 참고

* 전체 해시맵 알고리즘 설명: [../hash-map.md](../hash-map.md)
* 문제 분석: [1.analysis.md](./1.analysis.md)
* 알고리즘 적용 근거: [3.reasoning.md](./3.reasoning.md)

