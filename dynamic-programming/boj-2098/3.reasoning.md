# Main.java 설계 근거 정리

이 문서는 **BOJ 2098 외판원 순회 문제**에 대한 최종 해답 코드인 `Main.java`가
앞서 정리한 **비트마스킹 동적 계획법 개념, 알고리즘 논의와 어떻게 정확히 대응되는지**를 설명한다.

---

## 1️⃣ 알고리즘 선택 근거: 비트마스킹을 사용한 동적 계획법 (DP)

### 🎯 DP 문제 판단 기준

**DP 조건 확인:**
1. ✅ **최적 부분 구조**: 현재 상태의 최소 비용 = min(다음 상태의 최소 비용 + 이동 비용)
2. ✅ **중복 부분 문제**: 같은 상태가 여러 경로에서 계산됨
3. ✅ **상태 공간 제한적**: N × 2^N (N ≤ 16이므로 계산 가능)
4. ✅ **제약 조건이 있는 최적화 문제**: 최소 비용 경로 찾기 + 각 도시를 정확히 한 번씩 방문

**→ 비트마스킹 DP 사용** ✅

### 🤔 자주 하는 오해: DP vs 그리디

**오해:** "부분의 최적해가 전체의 최적해의 부분이 맞을까? 그 선택 때문에 전체로 봤을 때는 훨씬 효율적인 해를 찾지 못하는 건 아닐까?"

**이 오해는 그리디 알고리즘에 대한 판단입니다!** ✅

**핵심 차이:**
- **그리디**: 하나의 선택만 고려 (가장 좋은 것) → 전체 최적해 보장 안 됨
- **DP**: 모든 가능한 선택을 고려하고, 각 선택 후의 최적해를 재귀적으로 계산 → 전체 최적해 보장됨

**구체적인 예시:**
```
그리디: 0번 도시에서 가장 가까운 도시(1번)만 선택
→ 전체 최적해를 보장하지 않음

DP: 0번 도시에서 모든 미방문 도시(1, 2, 3번)를 고려
→ 각 선택 후의 최적해를 재귀적으로 계산
→ 그 중 최소값 선택 → 전체 최적해 보장됨
```

---

### 🔑 DP 사고 과정: 상태 공간을 효율적으로 탐색하기

**왜 "현재 도시와 방문한 도시 집합"이라는 상태 정의를 했을까?**

**핵심 사고 과정:**

1. **목표: 모든 도시를 방문하는 최소 비용 경로 구하기**
   - 문제: "N개 도시를 모두 방문하고 시작 도시로 돌아오는 최소 비용"
   - 하지만 이것을 한 번에 구하기는 어려움

2. **전략: 상태 공간으로 나누기**
   - 전체를 한 번에 구하기 어려우니, **특정 상태(현재 위치, 방문한 도시)로 제한**
   - "모든 도시를 방문하는 경로" → 너무 크고 복잡
   - "현재 도시에서 방문한 도시 집합 상태일 때의 최소 비용" → **특정 상태에서의 부분 문제**

3. **부분 문제의 해를 조합하여 전체 문제 해결**
   - `dp[0][0001]` = 0번 도시에서 시작, 0번만 방문한 상태에서의 최소 비용 = 전체 문제의 답
   - `dp[1][0011]` = 1번 도시에 있고, 0, 1번을 방문한 상태에서의 최소 비용 = 부분 문제
   - `dp[2][0111]` = 2번 도시에 있고, 0, 1, 2번을 방문한 상태에서의 최소 비용 = 부분 문제
   - ...

**왜 이렇게 접근했는가?**

**전체 경로를 직접 구하려면:**
- 모든 가능한 경로 조합을 고려해야 함
- 0번 → 1번 → 2번 → 3번 → 0번
- 0번 → 1번 → 3번 → 2번 → 0번
- 0번 → 2번 → 1번 → 3번 → 0번
- ... (총 (N-1)! 개의 경로)
- 시간 복잡도: O((N-1)!) - 비효율적
  - **근거**: N개 도시를 방문하는 순열의 개수는 (N-1)!

**상태 공간으로 나누면:**
- "현재 도시와 방문한 도시 집합"이라는 작은 상태로 나눔
- 각 상태는 더 작은 상태의 해를 사용
- 시간 복잡도: O(N² × 2^N) - 효율적
  - **근거**: 상태 개수 N × 2^N, 각 상태 처리 시간 O(N)

**구체적인 예시:**

```
전체 문제: "0번에서 시작하여 모든 도시를 방문하고 0번으로 돌아오는 최소 비용"
→ 너무 크고 복잡함

상태 공간으로 나누기:
- "0번 도시, 0번만 방문" → dp[0][0001]
- "1번 도시, 0, 1번 방문" → dp[1][0011]
- "2번 도시, 0, 1, 2번 방문" → dp[2][0111]
- ...
- "0번 도시, 모든 도시 방문" → dp[0][1111] (시작 도시로 복귀)

각 상태는 더 작은 상태의 해를 사용:
dp[0][0001] = min(10 + dp[1][0011], 15 + dp[2][0101], 20 + dp[3][1001])
dp[1][0011] = min(9 + dp[2][0111], 10 + dp[3][1011])
...
```

**결론:**
- 전체 경로를 직접 구하기 어려우므로
- **특정 상태(현재 도시, 방문한 도시 집합)로 나누어 해결**
- 부분 문제의 해를 조합하여 전체 문제 해결
- 이것이 바로 DP의 핵심 사고 과정!

---

### 🔍 다른 접근 방식과의 비교

**왜 `dp[현재도시][방문한도시집합]`을 선택했을까?**

**다른 알고리즘 배제 이유:**
- ❌ **그리디**: 각 단계 최소 비용 선택이 최적해를 보장하지 않음
- ❌ **정렬**: 방향에 따라 비용이 다름 (비대칭 그래프)
- ❌ **위상정렬**: 사이클이 있는 그래프에 적용 불가
- ❌ **다익스트라**: 모든 도시를 정확히 한 번씩 방문하는 경로를 찾을 수 없음
- ❌ **BFS**: 가중치 그래프에서 최소 비용을 보장할 수 없음
- ⚠️ **DFS (백트래킹)**: 이론적으로 가능하지만 시간 복잡도 O((N-1)!)로 비효율적
- ✅ **비트마스킹 DP**: 메모이제이션으로 중복 계산 방지, 시간 복잡도 O(N² × 2^N)로 효율적

**핵심 차이:**
- DFS는 같은 상태를 여러 번 계산 (중복)
- DP는 같은 상태를 한 번만 계산하고 재사용 (메모이제이션)

---

## 2️⃣ 코드 구조와 알고리즘 논의의 1:1 대응

### ① DP 배열 정의

```java
int[][] dp = new int[N][1 << N];
// dp[현재도시][방문한도시집합] = 최소 비용
```

* `dp[current][visited]`: 현재 도시에서 방문한 도시 집합 상태일 때, 나머지 도시를 모두 방문하고 시작 도시로 돌아오는 최소 비용
* `visited`: 비트마스킹으로 표현된 방문한 도시 집합 (예: 1011 = 0, 1, 3번 도시 방문)
* 배열 크기: N × 2^N

### ② 비트마스킹 사용

**왜 비트마스킹을 사용하는가?**

- 공간 효율적: 하나의 정수로 집합 표현 (boolean 배열 대비)
- 시간 효율적: 비트 연산은 O(1)
- DP 배열 인덱스로 직접 사용 가능

> **자세한 비트마스킹 설명**: [2.algorithm.md](./2.algorithm.md)의 "비트마스킹 표현식 구체적인 예시" 섹션 참고

### ③ 메모이제이션 구현

**왜 메모이제이션을 사용하는가?**

같은 상태가 여러 경로에서 계산되므로, 한 번 계산한 값을 저장하여 재사용합니다.

**구현:**
```java
// -1로 초기화 (미계산 상태)
for (int i = 0; i < N; i++) {
    Arrays.fill(dp[i], -1);
}

int solve(int current, int visited) {
    // 이미 계산된 경우
    if (dp[current][visited] != -1) {
        return dp[current][visited];  // 저장된 값 재사용
    }
    
    // 계산 후 저장
    dp[current][visited] = ...;
    return dp[current][visited];
}
```

### ④ 점화식 구현 (상태 공간 해석에서 도출)

**"현재 위치와 방문한 도시 집합" 해석이 점화식으로 이어지는 과정:**

```java
int solve(int current, int visited) {
    // 모든 도시를 방문한 경우
    if (visited == (1 << N) - 1) {
        // 시작 도시(0)로 돌아오는 비용
        return W[current][0] != 0 ? W[current][0] : INF;
    }
    
    // 이미 계산된 경우
    if (dp[current][visited] != -1) {
        return dp[current][visited];
    }
    
    dp[current][visited] = INF;
    
    // 모든 미방문 도시로 이동
    for (int next = 0; next < N; next++) {
        // 이미 방문한 도시는 건너뛰기
        if ((visited & (1 << next)) != 0) continue;
        
        // 갈 수 없는 경로는 건너뛰기
        if (W[current][next] == 0) continue;
        
        // 다음 상태로 이동
        int nextVisited = visited | (1 << next);
        int cost = W[current][next] + solve(next, nextVisited);
        
        dp[current][visited] = Math.min(dp[current][visited], cost);
    }
    
    return dp[current][visited];
}
```

**💡 시작 도시 고정:** TSP는 순환 경로이므로 어떤 도시에서 시작하든 최소 비용은 같습니다. 따라서 0번 도시에서만 시작하면 됩니다.

> **자세한 설명**: [2.algorithm.md](./2.algorithm.md)의 "시작 도시 고정" 섹션 참고

**점화식의 의미:**
1. **모든 도시 방문 완료**: 시작 도시로 돌아오는 비용만 계산
2. **미방문 도시 존재**: 현재 도시에서 모든 미방문 도시로 이동하는 경우를 고려
   - 이동 비용 + 다음 상태의 최소 비용
   - 모든 경우 중 최소값 선택

**경계 조건:**
* `visited == (1 << N) - 1`: 모든 도시를 방문했는지 확인
* `(visited & (1 << next)) != 0`: 이미 방문한 도시인지 확인
* `W[current][next] == 0`: 갈 수 없는 경로인지 확인

---

## 3️⃣ 비트마스킹 연산 처리

비트마스킹 연산을 사용하여 방문 집합을 효율적으로 관리합니다.

> **자세한 비트마스킹 설명**: [2.algorithm.md](./2.algorithm.md)의 "비트마스킹 표현식 구체적인 예시" 섹션 참고

---

## 4️⃣ DP의 최적 부분 구조

### 최적 부분 구조의 의미

현재 상태의 최소 비용은 다음 상태의 최소 비용을 사용하여 계산됩니다:

```
dp[현재도시][방문집합] = min(
    모든 미방문 도시 next에 대해:
    W[현재도시][next] + dp[next][방문집합 | (1 << next)]
)
```

이 점화식은 더 작은 문제의 최적값을 사용하므로, 최적 부분 구조를 만족합니다.

> **실제 계산 과정**: [2.algorithm.md](./2.algorithm.md)의 "실제 계산 과정 (N=4 예시)" 섹션을 참고하세요.

---

## 5️⃣ 완전 탐색과의 비교

**완전 탐색 (DFS/백트래킹):**
- 시간 복잡도: O((N-1)!) - 비효율적
- 같은 상태를 여러 번 계산 (중복)

**DP (메모이제이션):**
- 시간 복잡도: O(N² × 2^N) - 효율적
- 같은 상태를 한 번만 계산하고 재사용

**효율성 차이:** N=16일 때, 완전 탐색은 약 1.3 × 10^12번 계산, DP는 약 1.6 × 10^7번 계산

---

## 6️⃣ 시간/공간 복잡도와 코드의 대응

> **시간/공간 복잡도 분석**: [2.algorithm.md](./2.algorithm.md)의 시간/공간 복잡도 섹션을 참고하세요.

**코드와의 대응:**
- 시간 복잡도 O(N² × 2^N): 상태 개수 N × 2^N, 각 상태 처리 시간 O(N)
- 공간 복잡도 O(N × 2^N): `int[][] dp = new int[N][1 << N]` 배열 사용

---

## 7️⃣ 다른 접근 방법과의 비교

**비트마스킹 DP (현재 방법):**
- ✅ 시간 복잡도 O(N² × 2^N), 최적해 보장

**완전 탐색:**
- ❌ 시간 복잡도 O((N-1)!)로 비효율적

**그리디:**
- ❌ 최적해를 보장하지 않음

---

## 8️⃣ 코드 최적화 고려사항

### 현재 구현의 특징

* **간단하고 명확함**: 알고리즘의 의도가 코드에 잘 드러남
* **메모리 효율적**: 필요한 공간만 사용
* **시간 효율적**: O(N² × 2^N) 시간 복잡도

### 추가 최적화 가능성

* 공간 최적화: 방문 집합의 대칭성 활용 (하지만 구현 복잡도 증가)
* 입력 최적화: BufferedReader 사용 (이미 사용 중)
* 현재 구현이 문제 제약 조건 내에서 충분히 효율적

---

## 9️⃣ 정리

이 `Main.java`는 단순히 정답을 출력하는 코드가 아니라,

* 문제의 **상태 공간 탐색 구조**를 비트마스킹 동적 계획법으로 모델링하고
* 알고리즘 문서에서 설명한 **최적 부분 구조**를 그대로 활용하며
* 제약 조건 내에서 효율적으로 동작하는

**설계 의도가 드러나는 해답 코드**이다.

즉, 설명 → 알고리즘 → 코드가
**하나의 사고 흐름으로 완전히 일치**하도록 작성된 최종 결과물이다.

---

## 🔟 핵심 설계 원칙

1. **효율성**: O(N² × 2^N) 시간 복잡도로 최적화
2. **명확성**: 코드만 봐도 알고리즘의 동작을 이해할 수 있음
3. **일관성**: 알고리즘 문서의 설명과 코드가 1:1로 대응
4. **간결성**: 불필요한 복잡성 없이 핵심만 구현

