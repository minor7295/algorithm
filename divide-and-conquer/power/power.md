# 분할정복을 이용한 거듭제곱 정리

## 1️⃣ 분할정복 거듭제곱이란?

**분할정복을 이용한 거듭제곱** 이란

> 거듭제곱 `a^n`을 계산할 때,
> 지수를 반으로 나누어 재귀적으로 계산하는 분할정복 알고리즘
> 을 의미한다.

즉, **"지수를 절반으로 나누어 계산하고 합치기"** 전략을 사용하는 효율적인 거듭제곱 알고리즘이다.

---

## 2️⃣ 언제 사용할까?

분할정복 거듭제곱은 다음과 같은 상황에서 사용된다:

* 큰 지수의 거듭제곱을 빠르게 계산할 때
* 모듈로 연산과 함께 사용할 때 (큰 수 처리)
* 행렬 거듭제곱 계산
* 피보나치 수열 빠른 계산

### 대표적인 예시

* **큰 수의 거듭제곱 계산** (예: 1629번 문제)
* **모듈로 거듭제곱** (암호학, 수학 문제)
* **행렬 거듭제곱** (피보나치 수열, 그래프 경로 개수)
* **재귀 관계식의 빠른 계산**

---

## 3️⃣ 분할정복 거듭제곱의 핵심 개념

### 📌 핵심 아이디어

* `a^n = a^(n/2) * a^(n/2)` (n이 짝수)
* `a^n = a * a^(n-1)` (n이 홀수)
* 지수를 절반으로 줄여가며 계산

### 📐 분할정복 거듭제곱의 구조

```
a^10 계산하기

a^10
  │
  ├─ n=10 (짝수)
  │
  ├─ Divide: a^10 = a^5 * a^5
  │
  ├─ Conquer: a^5 계산
  │   │
  │   ├─ n=5 (홀수)
  │   │
  │   ├─ Divide: a^5 = a * a^4
  │   │
  │   ├─ Conquer: a^4 계산
  │   │   │
  │   │   ├─ n=4 (짝수)
  │   │   │
  │   │   ├─ Divide: a^4 = a^2 * a^2
  │   │   │
  │   │   ├─ Conquer: a^2 계산
  │   │   │   │
  │   │   │   ├─ n=2 (짝수)
  │   │   │   │
  │   │   │   └─ a^2 = a * a
  │   │   │
  │   │   └─ Combine: a^2 * a^2 = a^4
  │   │
  │   └─ Combine: a * a^4 = a^5
  │
  └─ Combine: a^5 * a^5 = a^10
```

---

## 4️⃣ 분할정복 거듭제곱 알고리즘의 구조

### 기본 구현

```java
long power(long base, long exponent) {
    // Base Case
    if (exponent == 0) {
        return 1;
    }
    if (exponent == 1) {
        return base;
    }
    
    // Divide: 지수를 반으로 나눔
    long half = power(base, exponent / 2);
    
    // Conquer: 절반의 거듭제곱 계산
    long result = half * half;
    
    // Combine: 홀수인 경우 추가 곱셈
    if (exponent % 2 == 1) {
        result = result * base;
    }
    
    return result;
}
```

### 모듈로 연산 포함 버전 (큰 수 처리)

```java
long power(long base, long exponent, long mod) {
    // Base Case
    if (exponent == 0) {
        return 1 % mod;
    }
    if (exponent == 1) {
        return base % mod;
    }
    
    // Divide: 지수를 반으로 나눔
    long half = power(base, exponent / 2, mod);
    
    // Conquer: 절반의 거듭제곱 계산
    long result = (half * half) % mod;
    
    // Combine: 홀수인 경우 추가 곱셈
    if (exponent % 2 == 1) {
        result = (result * base) % mod;
    }
    
    return result;
}
```

---

## 5️⃣ 분할정복 거듭제곱의 특징

### ✅ 장점

* **매우 빠른 계산**: O(log N)
* **큰 지수 처리 가능**: 선형 시간 알고리즘보다 훨씬 빠름
* **모듈로 연산과 함께 사용**: 큰 수 문제 해결
* **메모리 효율적**: 추가 메모리 사용 적음

### ❌ 단점

* **재귀 오버헤드**: 함수 호출 비용 (반복문으로 최적화 가능)

---

## 6️⃣ 시간 복잡도 분석

### ⏱️ 각 단계: `O(1)`

* 나눗셈 연산: O(1)
* 곱셈 연산: O(1)
* 모듈로 연산: O(1)

### ⏱️ 재귀 깊이: `O(log N)`

* 매번 지수가 절반으로 줄어듦
* N → N/2 → N/4 → ... → 1
* 총 재귀 깊이: log₂(N)

### ✅ 전체 시간 복잡도

```
O(log N)
```

일반적인 선형 시간 알고리즘 O(N)보다 훨씬 빠르다.

---

## 7️⃣ 공간 복잡도

### ⏱️ 재귀 호출 스택: `O(log N)`

* 최대 재귀 깊이: O(log N)
* 각 재귀 호출마다 스택 프레임 생성

### ⏱️ 추가 메모리: `O(1)`

* 변수 몇 개만 사용

### ✅ 전체 공간 복잡도

```
O(log N)
```

---

## 8️⃣ 분할정복 거듭제곱의 구현 패턴

### 패턴 1: 기본 재귀 구현 (위에서 설명한 코드)

### 패턴 2: 반복문 구현 (최적화)

```java
long powerIterative(long base, long exponent, long mod) {
    long result = 1;
    base = base % mod;
    
    while (exponent > 0) {
        // 지수가 홀수이면 결과에 곱하기
        if (exponent % 2 == 1) {
            result = (result * base) % mod;
        }
        
        // 지수를 절반으로 줄이고, base를 제곱
        exponent = exponent >> 1;  // exponent /= 2
        base = (base * base) % mod;
    }
    
    return result;
}
```

### 패턴 3: 행렬 거듭제곱

```java
long[][] matrixPower(long[][] matrix, long exponent) {
    int n = matrix.length;
    
    // 단위 행렬로 초기화
    long[][] result = new long[n][n];
    for (int i = 0; i < n; i++) {
        result[i][i] = 1;
    }
    
    while (exponent > 0) {
        if (exponent % 2 == 1) {
            result = matrixMultiply(result, matrix);
        }
        matrix = matrixMultiply(matrix, matrix);
        exponent /= 2;
    }
    
    return result;
}
```

---

## 9️⃣ 분할정복 거듭제곱의 활용

### 📌 피보나치 수열 빠른 계산

피보나치 수열의 n번째 항을 O(log N)에 계산:

```java
// 행렬 거듭제곱을 이용한 피보나치 수열
// [F(n+1), F(n)]^T = [[1, 1], [1, 0]]^n * [F(1), F(0)]^T

long fibonacci(long n, long mod) {
    if (n <= 1) return n;
    
    long[][] base = {{1, 1}, {1, 0}};
    long[][] result = matrixPower(base, n - 1, mod);
    
    return result[0][0];
}
```

### 📌 그래프 경로 개수 계산

인접 행렬의 n제곱은 길이 n인 경로의 개수를 나타냄:

```java
// 그래프에서 정점 i에서 정점 j로 가는 길이 n인 경로의 개수
// = 인접 행렬의 n제곱의 (i, j) 원소

long[][] countPaths(long[][] graph, long pathLength, long mod) {
    return matrixPower(graph, pathLength, mod);
}
```

---

## 🔟 분할정복 거듭제곱 vs 다른 방법

### 거듭제곱 계산 방법 비교

| 방법 | 시간 복잡도 | 설명 |
|------|------------|------|
| **반복문** | O(N) | N번 곱하기 |
| **분할정복** | O(log N) | 지수를 절반으로 나누기 |
| **라이브러리** | O(log N) | Math.pow (부동소수점) |

### 언제 분할정복 거듭제곱을 사용할까?

* **큰 지수 계산** (예: 10^18)
* **모듈로 연산 필요** (큰 수 처리)
* **정수 정밀도 필요** (부동소수점 오차 없음)
* **행렬 거듭제곱**

---

## 1️⃣1️⃣ 주의사항

### 📌 오버플로우 방지

큰 수를 다룰 때는 모듈로 연산을 적절히 사용:

```java
// 잘못된 예: 오버플로우 발생 가능
long result = (half * half) * base;

// 올바른 예: 각 단계에서 모듈로 연산
long result = ((half * half) % mod * base) % mod;
```

### 📌 음수 지수 처리

```java
long power(long base, long exponent, long mod) {
    if (exponent < 0) {
        // 역원 계산 (mod가 소수인 경우)
        base = modInverse(base, mod);
        exponent = -exponent;
    }
    // ... 나머지 로직
}
```

---

## ✨ 한 줄 요약

> 분할정복을 이용한 거듭제곱은 **"지수를 절반으로 나누어 재귀적으로 계산하는"** 알고리즘으로, O(log N) 시간에 큰 지수의 거듭제곱을 효율적으로 계산할 수 있다.

